package com.kodilla.selenium.pom;

public class KodillaSearchPomTest {
    /*
    @BeforeEach
    public void setUp() {
         searchPom = new KodillaSearchPom();
    }

    @Test
    public void testSearchPage_CheckResultsCount() {
        String[] searchPhrases = {"NoteBook", "School", "Brand", "Business", "Gaming", "Powerful"};
        int[] expectedCounts = {2, 5, 3, 4, 3, 2};

        for (int i = 0; i < searchPhrases.length; i++) {
            int actualCount = searchPom.searchAndGetResultsCount(searchPhrases[i]);
            assertEquals(expectedCounts[i], actualCount, "Incorrect results count for search phrase: " + searchPhrases[i]);
        }
    }

    @Test
    public void testSearchPage_CheckCaseInsensitiveSearch() {
        String[] searchPhrases = {"notebook", "SCHOOL", "bRanD", "busiNESS", "gAMING", "PoWeRfUl"};

        for (String searchPhrase : searchPhrases) {
            int countWithUpperCase = searchPom.searchAndGetResultsCount(searchPhrase);
            int countWithLowerCase = searchPom.searchAndGetResultsCount(searchPhrase.toLowerCase());
            assertEquals(countWithUpperCase, countWithLowerCase, "Results count differs for case-insensitive search: " + searchPhrase);
        }
    }

    @AfterEach
    public void tearDown() {
        searchPom.close();
    }
*/
}
